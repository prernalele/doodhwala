import React from "react";
import "./Button.css";

const Button = () => {
  return (
    <button type="submit" className="buttonClass">
      Add New Expense
    </button>
  );
};

export default Button;
